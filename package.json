{
  "name": "capi-netta-rp",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "deploy": "node deploy-commands.js",
    "start": "node index.js",
    "prod": "pm2 start ecosystem.config.js",
    "postinstall": "prisma generate",
    "setup": "npm install && npx prisma generate && node deploy-commands.js && npm run prod"
  },
  "dependencies": {
    "@prisma/client": "^5.10.0",
    "canvas": "^2.11.2",
    "discord-html-transcripts": "^3.2.0",
    "discord.js": "^14.13.0",
    "dotenv": "^16.3.1",
    "ejs": "^3.1.9",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "prisma": "^5.10.0"
  }
}